## Manual Orders

### Setup
* install ruby. If you use [asdf](https://github.com/asdf-vm/asdf) just run `asdf install` in the root folder
* install dependencies with `bundle install`

### Running
* check the examples in the filler.rb file
* run with `ruby mexc/filler.rb`
